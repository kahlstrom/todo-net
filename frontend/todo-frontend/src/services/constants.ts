/**
 * Application-wide constants.
 */

/**
 * LocalStorage keys for auth data.
 * Centralized to prevent typos and make changes easier.
 */
export const STORAGE_KEYS = {
  TOKEN: "token",
  USER: "user",
} as const;
